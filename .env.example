# App (used for docs; reset emails use window.location.origin)
NEXT_PUBLIC_APP_URL=http://localhost:3001

# Supabase
# Project ref: frzuiqafemdmmkqivewi
NEXT_PUBLIC_SUPABASE_URL=https://frzuiqafemdmmkqivewi.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# Tenant onboarding creates a real org + workspace per user (no bootstrap slug).

# LLM (server only - never NEXT_PUBLIC)
LLM_PROVIDER=claude
ANTHROPIC_API_KEY=
ANTHROPIC_MODEL=claude-sonnet-4-5

# Email (optional - Resend; when unset, Supabase sends invite emails)
RESEND_API_KEY=
RESEND_FROM_EMAIL="Social Intelligence <onboarding@yourdomain.com>"

# Cron (server only - protects /api/cron/* routes)
CRON_SECRET=
